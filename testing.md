# Testing

To ensure that integration tests continue to work while using captcha.eu protection, detect the unit, integration, or automation test and mock the response from the validation endpoint (or skip the call entirely).

